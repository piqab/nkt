upstream stand_app {
    least_conn;
    server app-a:8080 max_fails=3 fail_timeout=10s;
    server app-b:8080 max_fails=3 fail_timeout=10s;
    keepalive 16;
}

# Пул объявлен, но нигде не используется — правило upstream-orphan должно сработать.
upstream stand_legacy {
    server app-a:9999;
}
