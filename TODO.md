# TODO

Функционал, обсуждённый и отложенный на потом (не находки ревью).

- [ ] Английский язык интерфейса — нужен для реальных англоязычных
      пользователей продукта, работающих параллельно с русскими и надолго,
      поэтому i18n в этом же репозитории, а не форк в отдельный
      англоязычный (форк означал бы вручную переносить в него каждую
      будущую фичу навсегда). Решено разбить на два этапа:
      1. Фронтенд — вынести строки в react-i18next, добавить переключатель
         языка, перевести на английский.
      2. Бэкенд — сообщения API, которые сейчас возвращаются напрямую и
         показываются в интерфейсе (например ошибки `writeError`), тоже
         должны стать локализуемыми — язык в запросе, каталог сообщений в
         Go; обычные фронтенд-i18n-библиотеки эту часть не решают.
      Вывод внешних утилит (`nginx -t`, `certbot`, `haproxy -c`) переводу
      не подлежит в принципе — не в нашей власти, остаётся как есть.
- [ ] Автоматические проверки на уязвимости и актуализация зависимостей —
      сейчас в `.github/workflows/` только `release.yml` (сборка по тегу),
      проверок безопасности нет вообще. План (порядок внедрения свободный,
      пункты независимы друг от друга):
      1. `.github/dependabot.yml` — ecosystem-блоки `gomod` (корень), `npm`
         (`web/`), `github-actions` и `docker` (оба Dockerfile). Раз в
         неделю, группировка минорных/патч-апдейтов в один PR. Сам ничего
         не мержит, только открывает PR — самое дешёвое и полезное, начать
         стоит с этого.
      2. Новый `.github/workflows/security.yml` — по push/PR в main + раз
         в неделю по cron (чтобы ловить свежие CVE и без изменений в
         коде): `govulncheck ./...` для Go (сверяет реально используемые
         функции с базой уязвимостей, не просто версии пакетов) и
         `npm audit --audit-level=high` в `web/`. Без внешних секретов,
         только встроенный `GITHUB_TOKEN`.
      3. CodeQL — статический анализ самого кода (Go + JS/TS), не только
         зависимостей. Для старта — «Default setup» одной кнопкой в
         Settings → Code security (без файла workflow вообще), позже
         можно заменить на `.github/workflows/codeql.yml` для более
         тонкого контроля путей/расписания.
      4. Trivy — отдельный шаг в job `docker-hub-image` (`release.yml`),
         после `docker/build-push-action`, сканирует собранный образ
         `nkt-hub` перед публикацией в GHCR; `exit-code: 1` при
         critical/high — тогда уязвимый образ просто не уйдёт в релиз.

Остаток находок ревью безопасности хаба от 2026-08-12. Критичную (viewer
получал избыточный доступ к подключённым хостам через прокси) уже закрыли.
Ниже — то, что осталось, приоритет сверху вниз. Конкретные файлы/строки и
сценарии эксплуатации намеренно не хранятся здесь в открытом виде — детали
можно поднять по дате ревью, если понадобятся для самой работы над пунктом.

- [ ] SSH: подключение к управляемому хосту принимает его ключ без сверки
      (доверие при первом подключении, без pinning) — стоит сохранять
      отпечаток при первом успешном подключении и требовать явного
      подтверждения при его смене, как обычный `ssh`.
- [ ] Распаковка загружаемого архива (самоустановка Go-тулчейна) не
      проверяет цели symlink-записей внутри — источник доверенный (HTTPS),
      риск теоретический, но стоит применить ту же защиту путей, что уже
      есть рядом для самих записей.
- [ ] Файл базы данных хаба стоит открывать с явными правами доступа, а не
      полагаться на umask ОС.

Остаток находок более раннего общего ревью безопасности. Эксплуатируемых
уязвимостей нет — варианты усиления defence-in-depth, приоритет сверху вниз.

- [ ] CSRF-защита сессии — только `SameSite=Lax`, без отдельного
      токена или проверки `Origin`/`Sec-Fetch-Site`.
- [ ] Нет заголовков `Content-Security-Policy` и `Strict-Transport-Security`.
- [ ] Троттлинг попыток входа — по логину и только в памяти процесса (не по
      IP, не переживает перезапуск).
- [ ] Один из хендлеров управления контейнерами проверяет имя блок-листом
      символов вместо allowlist-регулярки, как везде рядом — практической
      дыры нет (фреймворк маршрутизации уже отсекает опасные значения), но
      паттерн внутренне непоследователен.
- [ ] Один внутренний хелпер формирует shell-команду конкатенацией строки —
      сейчас в неё попадают только зашитые литералы, риска нет, но паттерн
      опасен, если когда-нибудь получит аргумент из запроса.
- [ ] Редактор конфигов не резолвит symlink при проверке пути внутри
      разрешённых каталогов — эксплуатация требует стороннего доступа на
      запись в файловую систему, не через сами эти обработчики.
- [ ] В TUI не все поля сертификата экранируются перед выводом на экран —
      не RCE, но может испортить отображение.
