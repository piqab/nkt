# TODO

Функционал, обсуждённый и отложенный на потом (не находки ревью).

- [ ] Добить остальные места с тем же багом, что был в «Контейнерах»:
      `useApi()`'s `reload()` теперь awaitable (`web/src/api.ts`), баг
      «спиннер гаснет раньше, чем реально обновились данные» пофикшен
      в Docker/Podman/LXD/Virtualization/Services.tsx. Тот же паттерн
      (`x.reload()` без await перед сбросом busy/loading) остался в
      Firewall.tsx (~8 мест), Configs.tsx (~6 мест), Availability.tsx —
      надо пройтись и добавить `await`, ничего больше менять не нужно.
- [x] Английский язык интерфейса — завершено целиком, оба этапа.
      Нужен был для реальных англоязычных пользователей продукта,
      работающих параллельно с русскими и надолго, поэтому i18n в этом же
      репозитории, а не форк в отдельный англоязычный. Вывод внешних
      утилит (`nginx -t`, `certbot`, `haproxy -c`, `ss`, `ip`, `virsh`,
      `lxc`...) переводу не подлежит в принципе — не в нашей власти,
      остаётся как есть везде, где встречается.

      Этап 1 (фронтенд, react-i18next) — весь интерфейс переведён, 981
      ключ в `web/src/i18n/{ru,en}.json`:
      - [x] `react-i18next`/`i18next` подключены, ключи по неймспейсам,
            выбор языка в `localStorage` (`nkt-lang`), переключатель — и в
            сайдбаре, и на `Login.tsx` (`hooks/useLang.ts`).
      - [x] Все страницы и переиспользуемые компоненты переведены,
            включая не-компонентные модули (`api.ts`, `exportCrypto.ts`,
            `notifications.ts` — через прямой `i18n.t()`).

      Этап 2 (бэкенд, свой каталог сообщений в Go) — 229 ключей в
      `internal/msgs/{ru,en}.go`, язык передаётся заголовком `X-NKT-Lang`
      (HTTP, `web/src/api.ts`) или query-параметром `lang` (WebSocket,
      `usePty.ts`'s `wsURL`, браузер не даёт кастомных заголовков на
      handshake). Пять фаз:
      - [x] Фаза 1 — `internal/msgs` (каталог + `msgs.Errorf`/`*msgs.Err`
            для структурных ошибок), `writeError`/`fail` в `internal/api`
            и `internal/hub`, ~55 прямых литералов конвертированы. Деп
            `fmt.Errorf` (188 мест) внутри пакетов сознательно не тронуты
            — конвертация без искажения контекста обёрток (`%w`) требует
            по-пакетного аудита, не сделано ни в этой, ни в следующих
            фазах.
      - [x] Фаза 2 — `control.WriteResult.Message` (сохранение/откат
            конфига) — `Write`/`WriteBlock`/`Rollback` получили параметр
            `lang msgs.Lang`, TUI передаёт `msgs.RU` явно (свой язык не
            знает).
      - [x] Фаза 3 — прогресс установки хоста (`internal/hub/manager.go`
            и др.) и продления/выпуска сертификата (`internal/control/
            certgen.go`) в модалках логов. `Event`/`RenewEvent` получили
            `Key`/`Args` (`json:"-"`) рядом с `Text`, резолвятся в
            обработчике (`handleInstallJobStatus`/`handleRenewJobStatus`)
            на язык ИМЕННО текущего опроса, не языка старта job'а. certgen
            дополнительно поддерживает сырой непереводимый текст (вывод
            certbot) через `*certProgress{msg, raw}`.
      - [x] Фаза 4 — статусы источников/сертификатов
            (`model.SourceStatus.Error`, `model.Certificate.Error`,
            `model.RenewalInfo.Detail`) из `internal/parse` (~30 мест, 13
            файлов). Генерируются один раз при сканировании и кэшируются
            (`Scanner.Latest`) — поэтому текст не переводится в момент
            сканирования, а хранит `Key+Args` (`model.TextRef`),
            резолвится заново на каждый запрос через новый
            `internal/model/localize.go`'s `LocalizeSnapshot(lang, snap)`
            — вызывается в начале `handleOverview`/`handleFindings`/
            `handleCertificates`/`handleInventory`, кэш не трогает.
      - [x] Фаза 5 — диагностические находки (`model.Finding.Title/
            Detail/Suggestion`) из `internal/analyze` — все 35 правил, тем
            же аддитивным `Key+Args`-паттерном, тем же `LocalizeSnapshot`.
            `Finding`'s `Renewal.Detail` переиспользует уже переведённый
            `cert.Renewal.DetailKey/Args` из фазы 4 напрямую.

      Сознательно не переведено (задокументировано в коде, не забыто):
      составной текст `certs.go`'s `markDerivedCertbotCerts` и
      `analyze.go`'s `renewalState()` (конкатенация нескольких уже
      локализованных фрагментов — резолв одного ключа как аргумента
      другого `LocalizeSnapshot` не поддерживает), `docker.go`'s более
      глубокие `Warnings`-хелперы (`dockerFromEngine`/`dockerNetworks`/
      `parseCompose` — ошибка возвращается как `error` без `TextRef`),
      `model.Container.Status`/`model.Endpoint.Label` (не получили
      `Key`/`Args` полей).
- [ ] Страница «Уязвимости» — централизованная сверка для управляемых
      хостов хаба. Сейчас (`internal/vuln`, `internal/api/handlers_
      vulnerabilities.go`) работает для standalone nkt и для localhost
      хаба: каждый качает свою копию trivy + базу уязвимостей (~1ГБ) и
      сканирует себя сам. Для РЕАЛЬНЫХ управляемых хостов через SSH это
      пока не централизовано — каждый такой хост при сканировании тоже
      скачает свою полную копию базы, что и должны были избежать заводя
      этот дизайн изначально. Нужно:
      1. Новый лёгкий эндпойнт на хосте `GET /api/system/pkg-manifest`,
         отдающий сырые `internal/parse.Manifest` (три маленьких файла,
         уже собираются).
      2. На хабе — забрать этот манифест через уже существующий dial/
         session-механизм (как `pollHost` уже делает для findings), и
         прогнать `vuln.Scan` ЛОКАЛЬНО на хабе против его же кэша базы —
         без скачивания чего-либо на сам управляемый хост.
- [ ] Страница «Уязвимости» — сканирование образов LXD. Docker/Podman уже
      сделаны (`vuln.ScanImage`, `trivy image --image-src docker,podman`
      напрямую по локальному сокету, без выкачивания образа заново —
      проверено вживую и на реальном локальном контейнере). LXD технически
      не подходит под тот же механизм: там нет OCI-образов в привычном
      смысле (см. разведку в этой сессии — `LXDInstance` вообще не хранит
      ссылку на образ), нужен свой путь — вероятно `trivy rootfs` против
      файловой системы инстанса (`lxc file pull`/`exec`), ближе к тому, как
      уже устроено сканирование пакетов ОС хоста, чем к `trivy image`.

Остаток находок ревью безопасности хаба от 2026-08-12. Критичную (viewer
получал избыточный доступ к подключённым хостам через прокси) уже закрыли.
Ниже — то, что осталось, приоритет сверху вниз. Конкретные файлы/строки и
сценарии эксплуатации намеренно не хранятся здесь в открытом виде — детали
можно поднять по дате ревью, если понадобятся для самой работы над пунктом.

- [ ] SSH: подключение к управляемому хосту принимает его ключ без сверки
      (доверие при первом подключении, без pinning) — стоит сохранять
      отпечаток при первом успешном подключении и требовать явного
      подтверждения при его смене, как обычный `ssh`.
- [ ] Распаковка загружаемого архива (самоустановка Go-тулчейна) не
      проверяет цели symlink-записей внутри — источник доверенный (HTTPS),
      риск теоретический, но стоит применить ту же защиту путей, что уже
      есть рядом для самих записей.
- [ ] Файл базы данных хаба стоит открывать с явными правами доступа, а не
      полагаться на umask ОС.

Остаток находок более раннего общего ревью безопасности. Эксплуатируемых
уязвимостей нет — варианты усиления defence-in-depth, приоритет сверху вниз.

- [ ] CSRF-защита сессии — только `SameSite=Lax`, без отдельного
      токена или проверки `Origin`/`Sec-Fetch-Site`.
- [ ] Нет заголовков `Content-Security-Policy` и `Strict-Transport-Security`.
- [ ] Троттлинг попыток входа — по логину и только в памяти процесса (не по
      IP, не переживает перезапуск).
- [ ] Один из хендлеров управления контейнерами проверяет имя блок-листом
      символов вместо allowlist-регулярки, как везде рядом — практической
      дыры нет (фреймворк маршрутизации уже отсекает опасные значения), но
      паттерн внутренне непоследователен.
- [ ] Один внутренний хелпер формирует shell-команду конкатенацией строки —
      сейчас в неё попадают только зашитые литералы, риска нет, но паттерн
      опасен, если когда-нибудь получит аргумент из запроса.
- [ ] Редактор конфигов не резолвит symlink при проверке пути внутри
      разрешённых каталогов — эксплуатация требует стороннего доступа на
      запись в файловую систему, не через сами эти обработчики.
- [ ] В TUI не все поля сертификата экранируются перед выводом на экран —
      не RCE, но может испортить отображение.
