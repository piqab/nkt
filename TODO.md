# TODO

Функционал, обсуждённый и отложенный на потом (не находки ревью).
Обсуждено 2026-09-19: связь узлов кластера на хостах в разных сетях и
Cilium ClusterMesh.

## Как сейчас связываются узлы кластера на разных хостах

Три режима сети между хостами (`internal/hub/cluster_wg.go`,
`internal/control/wgmesh.go`); хаб во всех — только оркестратор, трафик
узлов через хаб не идёт.

- **NAT** — только один хост.
- **Мост** — машины берут адреса из сети хостов; нужен один L2-сегмент.
- **WireGuard** — full mesh между хостами: `wg-quick@nktwg<N>`
  (`10.200.<N>.<i>/24`), сеть машин `10.<100+N>.<i>.0/24` на хост,
  подсети соседей в `AllowedIPs`, `PersistentKeepalive = 25`, endpoint —
  адрес хоста из списка или «адрес для соседей».

| Случай | Сейчас |
|---|---|
| Хосты в разных подсетях, подсети маршрутизируются друг к другу | Работает: WireGuard, между хостами должен проходить UDP 51820; если хаб ходит на хост по адресу, которого сосед не видит — «адрес для соседей». |
| Хосты с белыми IP | Работает: WireGuard, endpoint = белый IP, UDP 51820 открыть во внешнем файрволе. |
| Подсети друг друга не видят | **Не поддерживается**: WireGuard требует прямого UDP-пути, обхода через хаб нет — создание падает на проверке связи по туннелю. |

## Фаза A — WireGuard «через хаб» (звезда)

- [ ] Новый вариант сети «WireGuard через хаб»: хаб сам peer с
      интерфейсом `nktwg<N>` (`10.200.<N>.254`), у каждого хоста один
      peer — хаб — с `AllowedIPs` = подсети всех остальных хостов; хаб с
      `ip_forward=1` перекладывает пакеты между peer-ами. Хостам нужно
      достать только UDP 51820 хаба, друг друга видеть не надо.
- [ ] Реализация: хаб применяет `WGManager` к себе как к хосту
      `localhost` (тот же API `/api/vm/wgmesh`); требования на хабе —
      `wireguard-tools`, `CAP_NET_ADMIN` в юните, `/etc/wireguard` в
      `ReadWritePaths`; в Docker — `cap_add: NET_ADMIN`, `sysctls
      net.ipv4.ip_forward=1`, проброс UDP 51820.
- [ ] Сухой прогон: хаб может поднять wg (права, пакет), порт открыт, с
      каждого хоста достижим UDP-порт хаба; предупреждение, что весь
      межхостовой трафик кластера (pod-to-pod, etcd) идёт через хаб.
- [ ] Смешанный вариант (часть пар напрямую, часть через хаб)
      WireGuard не умеет — на одну подсеть один peer; режим либо full
      mesh, либо звезда. В форме — переключатель, в сценариях —
      `network wireguard-hub`.

## Фаза B — Cilium с параметрами для ClusterMesh

- [ ] `cilium install --set cluster.name=<имя> --set cluster.id=<ID
      кластера в хабе>` (1–255, уникальный) — сейчас не задаётся.
- [ ] Уникальные и непересекающиеся Pod/Service CIDR на кластер: сейчас
      у всех одинаковые (k3s `10.42.0.0/16`, kubeadm `10.244.0.0/16`).
      По номеру кластера: `--cluster-cidr/--service-cidr` (k3s),
      `--pod-network-cidr/--service-cidr` (kubeadm); поля в форме с
      автозначениями, в `InstallSpec`, в сценариях.
- [ ] Только для новых кластеров: переустановка Cilium в живом кластере
      — отдельная рискованная операция, не делаем.

## Фаза C — «Связать кластеры» (Cilium ClusterMesh)

- [ ] Один WireGuard mesh на группу хостов (`nktwg0`, сеть машин
      `10.100.<хост>.0/24` на хост, общая для всех кластеров на нём)
      вместо туннеля на кластер — иначе узлы разных кластеров не видят
      друг друга даже на одних хостах. Кластеры «мостом» или с белыми
      IP узлов связаны и так.
- [ ] Кнопка «связать кластеры» в разделе «Кластеры»: выбрать два и
      больше кластеров с Cilium → задание: скопировать секрет
      `cilium-ca` из первого в остальные (через kubeconfig-и хаба),
      `cilium clustermesh enable --service-type NodePort` в каждом,
      `cilium clustermesh connect` попарно (на control plane первого —
      временный объединённый kubeconfig), `cilium clustermesh status
      --wait`, итог в карточке кластера.
- [ ] Сухой прогон «связать»: Cilium в обоих, уникальные
      `cluster.id`/CIDR, ping между узлами разных кластеров (node-IP
      нужны для VXLAN и NodePort 32379 clustermesh-apiserver).
- [ ] Порядок: A → B → C, каждая фаза отдельным коммитом; WireGuard и
      Cilium вживую в песочнице не поднять — проверять генерацию
      конфигов, шаги, тесты, форму и сухой прогон, сам туннель и
      ClusterMesh — на реальном стенде.
