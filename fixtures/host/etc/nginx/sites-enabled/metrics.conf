server {
    listen 80;
    server_name metrics.example.com;

    access_log /var/log/nginx/metrics.access.log main;

    location / {
        proxy_pass http://127.0.0.1:9090;
        proxy_set_header Host $host;
    }
}
