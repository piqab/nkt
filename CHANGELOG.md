# История изменений NetKnownsThat

Таймлайн правок по коммитам репозитория, от первого до последнего.

## 2026-07-28

- `9e1fdad` Аудит конфигураций, карта сетевых ресурсов и дашборд управления хостом
- `f04e9ba` Убраны ложные срабатывания анализатора, найденные на живом стенде
- `dfc79dc` Пошаговая инструкция запуска и снижение требования к версии Go
- `e937bcc` Терминальный интерфейс в том же бинарнике

## 2026-07-29

- `ad30a51` Сборка продакшен-бинарника только в Linux-окружении
- `61d8b00` Описание целей make в README
- `377cd33` Разумные умолчания при запуске на боевом хосте
- `e3d018e` Сертификаты: сроки, покрытие имён и состояние автообновления
- `41f9ba5` Управление паролями: nkt passwd, nkt users, смена своего пароля в вебе
- `aafa432` Сверка живого сертификата с сокетом и выпуск самоподписанных
- `e541a65` Веб-страница управления учётными записями
- `1d91895` Разбор haproxy crt как каталога сертификатов (SNI)
- `db9c6e9` Перевыпуск certbot-сертификатов: вручную и автоматически

## 2026-07-30

- `9340cdd` Обнаружение производных certbot-сертификатов вне /etc/letsencrypt
- `aae7f0e` Исправление: определять производный certbot-сертификат без прямой ссылки
- `e5b8496` Перевыпуск: остановка nginx/haproxy для --standalone и пересборка копий
- `dcf533e` Ручная сборка PEM для haproxy из certbot; исправлен баннер ошибок

## 2026-08-01

- `ac408a4` Сборка PEM для haproxy: перезапись известного пути и сроки в списке lineage
- `3ea6867` Исправлен вызов certbot --standalone; спиннер на кнопках долгих действий
- `1387490` Исправление: certbot renew убивался общим 30-секундным таймаутом команд
- `ccf6d70` Окно прогресса продления: остановка/certbot/пересборка/запуск в реальном времени

## 2026-08-02

- `6136a86` Кука сессии по умолчанию только по HTTPS; связь сайт-порт-ресурс на карте; ручное пересканирование в Сервисах
- `9caaecc` IDN-имена сайтов: ASCII/punycode остаётся рабочим значением, рядом — сноска в Unicode
- `0060da4` Блочный CRUD для конфигов nginx/haproxy: структура файла и точечная правка (v1)
- `17ba138` Блоки docker-compose и создание новых файлов конфигурации
- `57a067b` Применение изменений docker-compose, переход из «Контейнеры» к конфигу, номера строк в редакторах
- `8091c56` Сводка находок на карте ресурсов; исправлен доступ к compose-файлу запущенного контейнера
- `5485786` Карта ресурсов компактнее; редактирование контейнера без перехода со страницы
- `cf0735c` Комментарий и «применить» — над текстом, а не под ним
- `0141096` Выбор пути для нового контейнера (дерево от /home); компактная панель узла на карте ресурсов
- `ec2272c` Создание папки в выборе пути; инфо-панель карты только по клику, фиксированной высоты
- `4b0fef8` Панель узла на карте — на одной строке с заголовком, если есть место
- `e82f85c` Исправлен отказ docker compose при создании нового пустого стека
- `7e9ceaa` Выпуск нового сертификата Let's Encrypt для домена без существующей lineage
- `47050cd` Сборка PEM для haproxy: класть новый файл в уже существующий crt-каталог

## 2026-08-11

- `a03a4a0` Обнаружение и управление Podman-контейнерами (фаза 1 из 3)
- `262a9d3` Обнаружение и управление LXD (фаза 2 из 3)
- `9c47aa5` Обнаружение и управление libvirt/QEMU через virsh (фаза 3 из 3)
- `0e767c5` Игнорировать .claude/ — локальное состояние инструмента, не часть проекта
- `941b847` README: первым разделом — только развёртывание на Linux
- `2c1cd28` Продление certbot: всегда --standalone, с проверкой порта и именем блокировщика
- `793ee1c` Добавлен bootstrap-build.sh: сборка без Docker без ручной установки Go/Node
- `57ccad4` Исправлен краш интерфейса на пустых/минимальных списках (nil-срез → null → .filter/.map падает)
- `dbbc81a` make build/web сами переключаются на bootstrap-build.sh, если нет Docker
- `80f95a2` Перенести bootstrap-build.sh внутрь Makefile как native-build, убрать файл
- `8beed0a` Docker — отдельной страницей; мастер создания VM с реальным диском
- `7c0249b` native-build: подхватывать уже установленные в $HOME/.local Go/Node сама
- `2d998fc` Добавлен режим nkt hub: управляющий центр для нескольких VPS по SSH

## 2026-08-12

- `c37b5a4` Хаб: запуск без Docker (systemd) по умолчанию; починена смена пароля
- `ca9427e` Хаб: редактирование хоста и внятная диагностика SSH-ключа
- `22e7fc1` Хаб: генерация SSH-ключа по умолчанию вместо вставки своего
- `189d6e2` Хаб: настраиваемый путь к go (NKT_HUB_GO_BIN) для установки без Docker
- `18f52fc` Хаб сам ставит себе Go, если настроенный не запускается
- `899908d` Хаб: установка через sudo для не-root SSH-пользователя
- `bbe1835` Хаб: стабильный пароль администратора хоста при переустановке
- `06d19e5` Хаб: можно отменить установку, зависшие хосты не остаются заблокированы
- `1a4a79b` Хаб сам чинит рассинхронизацию пароля администратора на хосте
- `148e066` Хаб: главный баг прокси (дублирующаяся cookie), не разлогинивать из-за хоста, верхняя панель хаба, журнал установки открывается заново
- `b1e096a` Хаб: кнопка «обновить» при расхождении версий; чинит гонку при заходе на хост
- `94d6d94` Версионирование по semver: файл VERSION, автобамп минорной при сборке
- `8948b53` Хаб: статус NOPASSWD-доступа в списке хостов и кнопка его снятия
- `f40dbfe` Мигрировать недостающие колонки hosts на уже существующих базах
- `91947e9` Подставлять готовую команду sudoers в ошибку установки, действия в два ряда
- `0289c74` Хаб: кнопки действий — своя строка на всю ширину под «Имя», а не колонка
- `97eaf7c` Хаб: усиление контроля доступа при проксировании на хосты
- `3b884a7` TODO: план дальнейшего усиления защиты хаба
- `55f6eec` Хаб: фоновый опрос findings/доступности подключённых хостов (Фаза 0)
- `d4aaf15` Хаб: отдать закэшированные findings/доступность через GET /api/hub/hosts (Фаза 1)
- `85acf4b` Фронтенд: инфраструктура antd + мост темы + App-шелл (Фаза 2)
- `e5da55b` Фронтенд: общие примитивы ui.tsx → обёртки над antd (Фаза 3)
- `67c0c69` Хаб: Reachable — *bool, а не bool, в ответе /api/hub/hosts
- `e543ffe` Хаб: Hosts.tsx на antd Table, колонка «Проблемы», уведомления (Фаза 4)
- `1624018` Фронтенд: батчи A+B на antd — Login/Audit/Users/Findings/Services/Firewall/Certificates/Availability (Фаза 5)
- `413ceba` Фронтенд: батч C на antd — Configs/Docker/Podman/LXD/Virtualization (Фаза 5)
- `916631f` Фронтенд: батч D на antd — Overview/Usage/Topology (Фаза 5, все 17 страниц)
- `834d8db` Фронтенд: уборка мёртвого CSS + мелкие доводки после миграции (Фаза 6)

## 2026-08-13

- `430dc21` Хаб: поиск исходников для кросс-компиляции + журнал сервиса в ошибке установки
- `acade23` Хаб: не требовать /etc/nginx и /etc/haproxy оба сразу в systemd-юните
- `469d237` Фронтенд: более компактный дизайн (antd compactAlgorithm + плотный CSS)
- `87e14c0` VERSION: 1.5.1, версия больше не бампается автоматически при сборке
- `d80e7bc` Раздел «Разное» + объединение Docker/Podman/LXD/ВМ в один пункт меню
- `5aebb56` Фикс: docker-compose.yml «файл не найден» + переполнение списка файлов
- `1594cc5` Веб-терминал: интерактивный shell на хосте прямо в браузере
- `68b11a8` Обновления пакетов дистрибутива на Overview + интерактивное apt-get upgrade
- `832efff` Хаб: переключатель веб-терминала per-host (переживает обновление хоста)
- `0bcdb55` Фикс: «обновить»/«переустановить» не перезапускал уже работающий nkt
- `16e02b4` Хаб: экспорт/импорт списка хостов для бэкапа и переноса
- `1ab2db5` Хаб: «экспорт с ключом» — одношаговый перенос между хабами с разными NKT_HUB_MASTER_KEY
- `6c2a9e8` Фикс: пустой чёрный терминал в модалке обновления пакетов
- `55824f0` Терминал: xterm.js откачен на стабильную 5.5.0 + аддоны, тест WS через прокси хаба, фикс навигации при выборе хоста
- `f815f2d` Терминал: чинит пустое окно — xterm открывался на скрытом контейнере
- `38f0e2f` Терминал в модалке обновления: не ждать один кадр — ждать реальный размер
- `ed492c0` Хаб: тест на реальный логин + WS-терминал через полный роутер
- `f051911` Терминал: настоящая причина пустого окна — Unicode11Addon без allowProposedApi
- `b75cf99` Терминал: окно «Обновить» ждёт монтирования контейнера, а не молча сдаётся
- `4b9e557` Терминал: моноширинный шрифт литералом + побег из sandbox юнита для shell/apt
- `3d5645e` Обновление пакетов: закрытие окна больше не убивает apt-get на хосте
- `85b4f55` Обзор: видно, идёт ли обновление пакетов, ещё до открытия окна
- `bb3528d` Терминал: по умолчанию всегда bash, если он есть, а не то, что в \$SHELL
- `644ed87` Хаб: смена галочки веб-терминала сама переустанавливает nkt на хосте

## 2026-08-14

- `a580c37` README переписан как статья, а не справочник
- `c7ff5c8` Обновление пакетов: сразу видно исход и хост пересканируется сам
- `7da3c90` Разное: что за процесс на самом деле — команда, владелец и происхождение
- `dd742a3` Разное: сбой ps/cgroup теперь виден в «Источниках», а не молчит
- `7b6778d` Хаб: показывает версию, которая реально работает на хосте
- `7a8ea85` systemd-юнит: донастройка прав для корректного просмотра процессов
- `d60e6d7` Карта ресурсов: включает «Разное» + зум колесиком мыши
- `157c2e5` Версия 1.6.0
- `d219d98` Карта ресурсов: подсветка полного пути, а не одного соседнего узла
- `58c0e9c` Карта ресурсов: убрано лишнее ребро host->сервис
- `889128a` Карта: 127.0.0.1 -> localhost на backend-адресах + обрезка длинных подписей рёбер
- `fbd9c40` Карта: разводит рёбра по высоте бокса вместо схлопывания в одну точку
- `5b16838` Новая страница «Сетевые интерфейсы» + «Разное» переехало на «Сервисы»
- `2bc04e1` Сетевые интерфейсы: LOWER_UP отдельно от UP + ошибки/потери пакетов
- `6dfeece` Firewall: у открытых сокетов видно правило ufw + добавить/удалить на месте
- `5b26a76` Firewall: правило для сокета видно и при выключенном ufw + защита от блокировки SSH
- `d5b8d4e` Сетевые интерфейсы: видно, к какой docker-сети принадлежит bridge
- `46d26fd` Карта ресурсов: разные цвета для рёбер, расходящихся из одного узла
- `0e2114e` Карта ресурсов: раскраска только у активного (выделенного) пути
- `e230fd3` Сетевые интерфейсы: физические сверху, мосты и veth ниже
- `165f2b4` Карта ресурсов: один и тот же путь — один цвет на всём протяжении
- `9d5c757` Firewall: если ufw не установлен — явное сообщение и кнопка «Установить»

## 2026-08-19

- `81b6fda` Makefile: native-build качает Go под 32-бит ARM (armv7l → armv6l)
- `a10ef07` Firewall: чиню регрессию — ufw_installed не доходил до фронтенда
- `064cfcc` systemd-юнит: certbot может писать в /etc,/var/lib,/var/log/letsencrypt
- `3fcd35b` Поддержка firewalld — вторая опора наравне с ufw
- `a709f33` Поддержка Caddy — третий reverse proxy наравне с nginx/haproxy
- `3a49f59` Makefile: native-build находит Node для 32-бит ARM (дропнут в 24+)
- `39558dd` Certbot: остановка для --standalone больше не валит рендер на caddy
- `8b9bb80` Сервисы: «Разное» убрано из вкладки, один раздел вместо двух
- `e87f8ce` Makefile: поднимаю лимит памяти Node для сборки фронтенда на 32-бит ARM
- `707408a` systemd-юнит: донастройка прав для выпуска сертификатов certbot
- `a87d31d` Сертификаты: карточка «Неподключённые» — certonly без установки в сервис
- `0558adc` go.mod: временный replace nginx-go-crossplane для сборки на 32-бит ARM
- `62c5c6d` Хаб: поддержка 32-бит ARM хостов, в любую сторону
- `2007145` Хаб: предупреждение перед перезаписью чужой установки nkt
- `fef8d5c` Хаб: сканирует саму себя как закреплённый хост «localhost»
- `ecf031f` Хаб: чиним 404 на всех разделах «localhost» (переиспользование chi route context)
- `5d7908e` Хаб: «открыть» на устаревшем хосте сначала обновляет его
- `917ed16` nkt/nkt hub: опциональный встроенный TLS с самоподписным сертификатом
- `ca8ef06` Хаб: фикс «error obtaining VCS status» при кросс-компиляции бинарника
- `7034878` nkt hub: команда для безвозвратного удаления хаба с затиранием ключей

## 2026-08-20

- `8a03de0` hub delete: чиним зависание, отдельный DataDir хаба, шифрование экспорта паролем
- `e00852b` Веб-интерфейс: «экспорт с ключом» тоже шифрует паролем (PBKDF2, общий с CLI формат)
- `0f9efeb` Хаб: резервный канал связи с хостом на случай недоступности SSH
- `ac79439` Резервный канал: ограничение попыток подбора токена (429 после 5 неудач)
- `9828152` Добавлен CHANGELOG.md — таймлайн всех коммитов от первого до последнего
- `1e0c2df` Обезличить формулировки уязвимостей в CHANGELOG.md и TODO.md
- `e54fad4` CI: автоматический релиз бинарников на GitHub Actions по тегу
- `6b035c0` First release v1.7.0
- `cf2def8` First release 1.7.0
- `b415037` CI: чекаут репозитория в job release — иначе gh release create падает
- `3a36d2b` Откат VERSION на 1.7.0 — тег переставляется на этот коммит
- `9982c17` README: разделён на установку и разработку, добавлены Docker/K8s для хаба
- `e0abfab` Резервный канал: теперь чинит и переустановку/обновление, включён по умолчанию
- `e2da16c` Резервный канал: чиню сброс сессии при "изменить" и авто-адрес хаба
- `aa15a37` Резервный канал: не отбрасывать приватные адреса хаба + предупреждать о явно недостижимой паре
- `9122b42` Резервный канал: развёрнут на хаб→хост вместо хост→хаб
- `5359880` Хаб: проксируемые WebSocket (терминал, обновление пакетов) всегда получали 403
- `45226bd` Терминал: откат на песочный шелл, если на хосте нет D-Bus
- `5cf8955` Терминал: кнопка «установить dbus» + запасной выход из песочницы через nsenter
- `f63f3dd` Резервный канал: гонка после рестарта хоста валила вход администратора
- `35e64e3` Хаб: терминал для "localhost" включён по умолчанию
- `97cdd10` Фикс localhost-терминала (давний баг) + таймаут чтения самообновления
- `d0bf8a6` Резервный канал: повторять отправку самообновления при временном i/o timeout
- `67299fa` Терминал: бэйдж D-Bus виден всегда и цветом отражает доступность
- `a20ca73` Резервный канал: закрепление сертификата хоста вместо слепого InsecureSkipVerify
- `7ea1445` D-Bus: проверять реальным запуском systemd-run, а не только сокетом
- `941ed39` Юниты: явно разрешить setns(2) — @system-service его исключает

## 2026-08-21

- `34d3390` Обзор: кнопка «обновить» видна всегда; apt-get dist-upgrade вместо upgrade
- `4e2b1a0` Резервный канал: не проваливать install() из-за гонки при переподключении
- `d557206` Сертификаты: сборка PEM для haproxy сама пересканирует хост
- `124986f` Конфигурации: под путём к файлу — какие сайты в нём объявлены
- `16d3c83` Конфигурации: клонирование файла под новым именем
- `372ce2e` LineChart: курсор не совпадал с точкой при не-900px ширине
- `9c5989b` Использование ресурсов: пунктирная линия-ориентир (всего памяти/ядер CPU)
- `fdfdec2` Терминал: открывался не в реальном размере окна + иногда обрывался
- `b764c9d` Клонирование файла подставляет путь; ось графика больше не растягивается под ориентир
- `79b44c4` Сервисы: enable/disable, логи по кнопке, kill для остальных сервисов
- `d5d8dba` Неактивные единицы вынесены отдельным списком (Сервисы/Docker/Podman/LXD/VM)
- `c5b55b4` Терминал: кнопка «Открепить в отдельное окно»
- `d6190b3` Открепленное окно терминала: не подключалось к хосту хаба, кнопка → закрыть
- `4b7838f` Открепление терминала: отдельное окно на каждый хост хаба
- `0964abb` Терминал: описание и блок D-Bus скрываются при успешном подключении

## 2026-08-22

- `2a6912d` Экспорт/импорт хостов: переносить галочку и токен резервного канала
- `e32dbd3` Proxy: недоступный хост отдавал голую "Ошибка 502" вместо причины
- `9cab903` «Экспорт с ключом»: токен резервного канала не перешифровывался

## 2026-08-24

- `9899120` Терминал: запускать shell от ssh_user хоста, а не от root
- `170cf8e` Терминал: кнопка «Открыть в tmux» с автоустановкой и шпаргалкой команд
- `eea07c8` Терминал: кликабельный список окон tmux вместо ввода команд
- `8ec8220` Терминал tmux: не создавать сессию внутри интерактивного PTY escape hatch
- `ef4f16f` Терминал tmux: убрать кликабельную панель окон, оставить статичную шпаргалку
- `fd35716` Терминал: шпаргалка tmux — читаемые компактные чипы вместо серых кнопок
- `90b23d9` Терминал: шпаргалка tmux — кнопка скрыть/показать, состояние запоминается
- `76c1921` Описания под заголовками — в иконку i вместо видимого текста
- `3bd7e04` TODO: план по i18n; CHANGELOG.md: коммиты 2026-08-20…2026-08-24
- `8a987ba` CHANGELOG.md: убран случайный дублирующийся заголовок ## 2026-08-20
- `687df53` TODO: план автоматических проверок на уязвимости (Dependabot/govulncheck/CodeQL/Trivy)
- `b278fb4` TODO: убран пункт про CI-безопасность репозитория — неверно понял вопрос
- `300856f` Уязвимости: сканирование пакетов ОС через trivy, новая страница
- `63d5c22` TODO: два оставшихся шага для «Уязвимости» — хаб-централизация и образы
- `e6d5aa9` Уязвимости: кнопка «Сканировать» молчала при отказе в доступе
- `96ccf83` Уязвимости: заметный индикатор сканирования, не только на кнопке
- `15de29e` Уязвимости: пагинация таблицы наверх, сброс на 1-ю страницу при фильтрах, 50/100/все
- `e23f74a` Уязвимости: спиннер не появлялся, когда скан завершался быстрее опроса статуса
- `f69a3e8` Уязвимости: помнить прошлый скан, показывать что нового и что исправлено
- `4ad12cd` Уязвимости: сканирование образов Docker/Podman, не только пакетов ОС
- `aafd296` Уязвимости: фильтр «Источник» не фильтровал — сентинель «все» совпадал с ''
- `19a0f60` Уязвимости: CVE ID — ссылка на источник (PrimaryURL от trivy)
- `0be4574` Уязвимости: ссылка на CVE не была видна, счётчики в фильтре «Источник»
- `7100cf1` CHANGELOG.md: коммиты по фиче «Уязвимости» (2026-08-24)

## 2026-08-25

- `a71b69a` Контейнеры/Сервисы: спиннер гас раньше, чем список реально обновлялся
- `49c7e8c` Уязвимости: результат скана переживает перезапуск процесса
- `36e9d35` Уязвимости: прогресс сканирования — в модальном окне, как в остальных местах
- `9da37f8` Контейнеры/Podman/LXD/VM/сервисы: state в списке обновляется сразу после старт/стоп
- `abaa44d` Уязвимости: спиннер/окно скана не гасли раньше времени из-за гонки
- `95fb8e3` CHANGELOG.md: коммиты 2026-08-25 (гонки спиннеров в Уязвимостях/Контейнерах)
- `46f2822` Модалки фоновых задач: клик по фону мгновенно закрывал окно без следа
- `a453d45` Уязвимости: переделан весь цикл опроса статуса скана — реальная причина глюков
- `ba3ee64` Хаб: заливка бинарника на хост показывает прогресс в процентах
- `43755e3` Хаб: прогресс заливки бинарника — одна обновляющаяся строка, не лог из сотен
- `4e50bbe` Уязвимости: «сканирование не запускалось» показывалось до первой загрузки
- `e4c5333` Уязвимости: таблица показывала находки не из выбранного источника
- `6241738` Хаб: кнопка «обновить» могла зависать со спиннером навсегда — гонка статусов

## 2026-08-26

- `2213f48` Хаб: заливка бинарника стала очень медленной — сам же прогресс её и тормозил
- `df8c72e` Английский интерфейс: инфраструктура i18n, переведён шелл + Login
- `085fb71` Английский интерфейс: переведены общие компоненты + 3 страницы
- `4f753c2` Английский интерфейс: переведены Interfaces.tsx и Users.tsx
- `ba1298f` Английский интерфейс: переведены LXD.tsx и Podman.tsx
- `be48237` Английский интерфейс: переведена Docker.tsx
- `c8695f1` Английский интерфейс: переведена Virtualization.tsx — контейнер-семейство завершено
- `ca30937` Английский интерфейс: переведена Hosts.tsx (1324 строки) — самый крупный файл
- `2b46928` Английский интерфейс: переведена Certificates.tsx (937 строк)
- `0ecf71a` Английский интерфейс: переведена Firewall.tsx (887 строк) — три приоритетных файла готовы
- `eb7f1af` Английский интерфейс: переведена Topology.tsx (611 строк)
- `7533798` Английский интерфейс: переведена Configs.tsx (531 строка)
- `3b7ff6b` Английский интерфейс: переведён charts.tsx — общий компонент графиков
- `780083e` Английский интерфейс: переведена Services.tsx (441 строка)
- `f4f0567` i18n: перевод Terminal.tsx (497 строк), ~45 новых ключей terminal.*
- `f2b6321` i18n: перевод Overview.tsx, 49 новых ключей overview.*
- `01ba682` i18n: перевод Vulnerabilities.tsx, 29 новых ключей vulns.*
- `a87e5ac` i18n: перевод Availability.tsx, 44 новых ключа availability.*
- `1028ecb` i18n: перевод Usage.tsx, 28 новых ключей usage.*
- `f3681ef` i18n: перевод BlockTree.tsx и PathPicker.tsx, 27 новых ключей
- `653154f` i18n: перевод exportCrypto.ts и notifications.ts, 6 новых ключей hosts.*
- `61270b0` i18n: переключатель языка на Login.tsx, добил пропущенный hub-topbar в App.tsx
- `e00a286` TODO.md: отметить завершение этапа 1 i18n (фронтенд)
- `7f75014` Хосты: переключатель темы/языка на экране списка хостов хаба, кнопка «Добавить хост» в шапку
- `c49578b` i18n этап 2, фаза 1: каталог сообщений бэкенда + writeError/fail на языке запроса
- `cb25b69` i18n этап 2, фаза 2: control.WriteResult.Message на языке запроса
- `c40dd52` i18n этап 2, фаза 3a: прогресс установки/обновления хоста на языке запроса
- `a8ad9c7` i18n этап 2, фаза 3b: прогресс продления/выпуска сертификата на языке запроса
- `157223f` i18n этап 2, фаза 4: статусы источников и сертификатов на языке запроса
- `14d4587` i18n этап 2, фаза 5: диагностические находки (Finding) на языке запроса
- `ac629f2` TODO.md: отметить i18n завершённым целиком (оба этапа)

## 2026-08-27

- `9dc68cd` Язык по умолчанию: только ru-браузер даёт русский, остальные — en; терминал переживает переход между разделами
- `10d75b1` Терминал: фокус при возврате на страницу, «скрыть» в tmux-подсказках прячет всю карточку
- `ef0d5bc` VERSION: 1.8.0 — минорная версия за i18n (фронтенд+бэкенд) и устойчивый терминал
- `2ab0e39` Терминал: keepalive-пинг теперь реально проверяет живость соединения
- `7189ffa` Терминал: keepalive не должен убивать сессию из-за свёрнутой вкладки
- `be786a8` Хаб: WS-эндпоинты (терминал и др.) больше не убиваются Timeout прокси

## 2026-08-28

- `a4e7ce7` Терминал: обратный отсчёт до закрытия сессии по неактивности
- `df0f9ee` Терминал: копирование нескольких строк не должно схлопываться в одну
- `e5bd054` Терминал: копирование без HTTPS + двойное контекстное меню в tmux
- `bed8c55` Терминал: копирование через OSC 52 (nvim, tmux copy-mode и т.п.)
- `7970764` CHANGELOG.md: коммиты 2026-08-25…2026-08-28 (i18n этап 2, устойчивость терминала, копирование)

## 2026-08-30

- `b2ebecd` Терминал tmux: «no sessions» при первом открытии — systemd убивал сервер
- `f2bcf77` Хосты: инструкция по passwordless sudo при ошибке установки не читалась
- `3754819` Хаб: инструкция NOPASSWD не показывалась на хосте с нерусской sudo... с русской локалью
- `af19f23` Терминал: управление x11vnc-сессией (установка, запуск/остановка)
- `7d07568` Сервисы: клик по неустановленному в сводке — установка сервиса
- `c736980` Обзор: панель распространённых пакетов — чекбоксы, установка/удаление
- `3447287` Терминал: VNC — кнопка в общем ряду вместо отдельной панели
- `cc36ed7` VNC: настоящая причина отказа запуска, не просто «exit status 1»
- `ef7d18e` Сервисы: чип неактивного сервиса — цветом различать «не установлен» и «остановлен»
- `1b05f06` Конфиги: редактирование fail2ban (jail.local/jail.d) как простого текста
- `c722109` Сервисы: чип «установлен, но остановлен» теперь запускает сервис по клику
- `3527e8c` VNC: NKT_VNC_USER — реальная причина отказа, найденная пользователем
- `c5e3af4` Хосты: пользователь рабочего стола (VNC) — настройка хоста, не переменная окружения

## 2026-08-31

- `32101aa` VNC: полное удаление — x11vnc так и не удалось надёжно запустить
- `4bb6d98` Пакеты: раскладка чипами в строку + больше инструментов в наборе
- `8e5a937` Пакеты: отдельная страница — поиск и установка любого apt-пакета, менеджер установленных
- `076913d` Пакеты: исправлен выбор количества строк на странице в таблице установленных
- `dc0413a` Обновление ОС перенесено в Пакеты; на главной хаба — последовательное «Обновить всё»
- `961a957` Консоль: стартовые сообщения хаба и обычного хоста — на английском по умолчанию
- `018d046` Консоль: остальные логи резервного канала и хаба — тоже на английском
- `db87acb` Нагрузка: вкладка с живым btop прямо в браузере
- `d7aa93c` btop: показывать настоящую причину, если сессию/установку не открыть
- `81e6e7f` CHANGELOG.md: коммиты 2026-08-30…2026-08-31 (VNC-сага и удаление, менеджер пакетов apt, Пакеты/Обновить всё, английская консоль, btop)
- `611e379` README: полностью переписан на английском, новая структура (обзор → запуск на хосте → хаб в Docker → всё остальное)
- `9ec7c36` TUI: двуязычный интерфейс (English/Русский) с выбором языка при первом запуске
- `e6d72fe` README: русская версия (README.ru.md) и переключатель языка вверху обоих файлов

## 2026-09-01

- `455a28a` Подготовка к открытию репозитория: LICENSE (MIT), module path на реальный адрес репозитория, доки под публичный ghcr-пакет
- `9cc3077` README: ссылки на релиз обновлены до актуальной версии
- `2c3aa8e` README/HUB.md: явный git clone + NKT_HUB_SOURCE_ROOT для хаба без Docker
- `8930890` Хаб: автозагрузка готового бинарника с GitHub Releases, если исходников нет; ошибка занятого порта теперь называет процесс

